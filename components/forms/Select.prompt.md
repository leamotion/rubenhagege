Labeled native select for short lists like part counts or timeslots.

```jsx
<Select label="Nombre de parts" value={parts} onChange={setParts}
  options={[{label:'4 parts',value:'4'},{label:'6 parts',value:'6'}]} />
```
