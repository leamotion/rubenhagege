Uppercase status pill for stock/availability notes.

```jsx
<Badge tone="accent">Uniquement le vendredi</Badge>
```

Tones: `muted` (default, neutral gray), `accent` (burgundy, urgent), `gold` (premium/limited-edition), `inverse` (on dark surfaces).
